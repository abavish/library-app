import Route from '@ember/routing/route';

export default Route.extend({
	// contactEmail: '',
	// isValid: match('contactEmail', /^.+@.+\..+$/),
	// notEmptyEmail: notEmpty('contactEmail'),

	//isDis: false,


});
